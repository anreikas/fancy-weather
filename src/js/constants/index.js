import { appClasses } from './appClasses';
import {
  ocDataApiKey, darkSkyApiKey, proxyUrl, flickerApiKey, yandexKey, langs, tempUnits,
  borderOfMonthName, iconPath, iconEx, enterKeyCode, langCodes, pronControlPhrases,
} from './constants';

import { siteLocalization, voc } from './siteLocalization';

export {
  appClasses, ocDataApiKey, darkSkyApiKey, proxyUrl, flickerApiKey, yandexKey,
  langs, tempUnits, siteLocalization, voc, borderOfMonthName, iconPath, iconEx,
  enterKeyCode, langCodes, pronControlPhrases,
};
